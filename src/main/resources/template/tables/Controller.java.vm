package ${keyController};

import lombok.extern.slf4j.Slf4j;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import club.newepoch.isf.api.BaseApi;
import ${keyApi}.I${className}Api;
import ${keyApplication}.${className}Application;


/**
 * 成都思致科技有限公司
 *
 * @author ${author}
 * @version V1.0
 * 说明：${comments}实体类对应Controller
 * =========================================================================
 * 变更履历：
 * -------------------------------------------------------------------------
 * 变更编号     变更时间    变更人   变更原因    变更内容
 * -------------------------------------------------------------------------
 * <p>
 * @date ${datetime}
 **/
@Slf4j
@RestController
@RequiredArgsConstructor
public class ${className}Controller extends BaseApi implements I${className}Api {

    private final  ${className}Application ${classname}Application;

}
