package ${package}.${moduleName}.interfaces.rest;

import ${package}.api.Constants;
import ${package}.${moduleName}.application.${className}Application;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import lombok.extern.slf4j.Slf4j;
import ${package}.api.${moduleName}.interfaces.rest.${className}Api;


/**
 * 成都思致科技有限公司
 *
 * @author ${author}
 * @version V1.0
 * 说明：${comments}实体类对应Controller
 * =========================================================================
 * 变更履历：
 * -------------------------------------------------------------------------
 * 变更编号     变更时间    变更人   变更原因    变更内容
 * -------------------------------------------------------------------------
 * <p>
 * @date ${datetime}
 **/
@Slf4j
@RestController
public class ${className}Controller implements ${className}Api {

    @Autowired
    private ${className}Application ${classname}Application;

}
