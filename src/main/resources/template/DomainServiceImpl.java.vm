package ${package}.${moduleName}.domain.${className}.service;

import ${package}.${moduleName}.domain.${classname}.repository.I${className}Repository;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;


/**
 * 成都思致科技有限公司
 *
 * @author ${author}
 * @version V1.0
 * 说明：${className}实体类对应service接口实现类
 * =========================================================================
 * 变更履历：
 * -------------------------------------------------------------------------
 * 变更编号     变更时间    变更人   变更原因    变更内容
 * -------------------------------------------------------------------------
 * <p>
 * @date ${datetime}
 **/
@Slf4j
@Service(value = "${className}DomainService")
public class ${className}Service {

    @Autowired
    I${className}Repository ${classname}Repository;

}